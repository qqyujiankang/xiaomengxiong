<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/start_or_end_15"
    android:paddingTop="@dimen/start_or_end_15"
    android:paddingRight="@dimen/start_or_end_15"
    android:paddingBottom="@dimen/start_or_end_15">

    <com.example.et.View.SwipeCaptchaView
        android:id="@+id/swipeCaptchaView"
        android:layout_width="300dp"
        android:layout_height="200dp"
        android:layout_centerHorizontal="true"
        android:scaleType="centerCrop"
        app:captchaHeight="30dp"
        app:captchaWidth="30dp"
        tools:src="@mipmap/pic8" />

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/swipeCaptchaView"
        android:layout_marginLeft="16dp"
        android:layout_marginTop="@dimen/icon_width_height_30"
        android:layout_marginRight="16dp"
        android:layout_marginBottom="6dp"
        android:clipChildren="false">

        <SeekBar
            android:id="@+id/dragBar"
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:background="@null"
            android:max="1080"
            android:progressDrawable="@drawable/drag_seek_progress"
            android:splitTrack="false"
            android:thumb="@drawable/thumb_bg" />

        <TextView
            android:id="@+id/drag_tv_tips2"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="按住按钮往右滑动,完成拼图验证"
            android:textColor="@color/white" />

    </FrameLayout>

</RelativeLayout>
